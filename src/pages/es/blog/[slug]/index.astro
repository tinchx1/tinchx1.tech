---
import BlogPost from "@/components/BlogPost.astro";
import blogPosts from "@/data/es.blogs.json";

const { slug } = Astro.params;
const post = blogPosts.find((p) => p.slug === slug);

if (!post) {
  throw new Error(`No se encontró la publicación: ${slug}`);
}
---

<BlogPost {...post} />
