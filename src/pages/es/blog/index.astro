---
import blogPosts from "@/data/es.blogs.json";
---

<h1>Blog</h1>
<ul>
  {
    blogPosts.map((post) => (
      <li>
        <a href={`/blog/${post.slug}`}>
          <h2>{post.title}</h2>
          <p>{post.seoDescription}</p>
        </a>
      </li>
    ))
  }
</ul>
